Задачи «Массивы»

11 класс

Задача №1

Задан упорядоченный по возрастанию целочисленный массив. Сформировать второй массив из случайных целых чисел, которые не встречаются в первом массиве, но имеют величину больше минимального и меньше максимального из чисел первого массива.

Задача №2

Дана матрица размера a х b. Найти суммы элементов всех ее четных строк и нечетных столбцов.

Задание №3

Дана квадратная матрица порядка М. Повернуть ее на 90, 180, 270 градусов в положительном направлении.

Задание №4

Дан массив размера N. Осуществить сдвиг элементов массива вправо на одну позицию.

Задание №5

Дана матрица размера a х b. В каждой строке найти количество элементов, больших среднего арифметического всех элементов этой строки.

Задание №6

Матрица, симметричная относительно главной диагонали, задана верхним треугольников в виде одномерного массива по строкам. Восстановить исходную квадратную матрицу и напечатать по строкам.

Задача №7

Квадратная матрица задана в виде одномерного массива по строкам. Напечатать верхний треугольник матрицы (включая элементы главной диагонали) по строкам.

Задание № 8

Дана матрица размера a х b. Преобразовать матрицу, поменяв местами минимальный и максимальный элемент в каждой а) строке б) столбце.

Задача №9

Дана матрица N х M. Переставляя ее строки и столбцы, переместить наибольший элемент в верхний левый угол. Определить можно ли таким же образом поместить минимальный элемент в нижний правый угол.

Задача №10

Заданы матрица (размером 5х5) и число k. Столбец с максимальным по модулю элементом в k-й строке переставить с k-ым столбцом.

Задача №11

Дана матрица размером 5х7. Найти сумму всех ее элементов.

Задача №12

Произвести сортировку массива M[1…20] по возрастанию значений элементов.

Задача №13

Просуммировать элементы столбцов заданной матрицы размером 5х5. Результат получить в одномерном массиве размером 5.

Задача №14

Дан целочисленный массив А размера 10. Вывести номер первого и последнего их тех его элементов A[i], которые удовлетворяют двойному неравенству: А[1]  A[i]  A[10]. Если таких элементов нет, то вывести 0.

Задача №15

Напишите программу, которая находит сумму цифр данного натурального числа N.

Задача №16

Дан массив А[1...N, 1…N]. Составить программу, которая прибавила бы каждому элементу данной строки элемент, принадлежащий этой строке и главной диагонали.

Задача №17

Напишите программу, которая переводит введенное число (в десятичной записи) в двоичную запись. Для этого заведите массив для хранения цифр, и вместо печати символов помещайте их в этот массив. Затем выведите содержимое массива в нужном порядке.

Задача №18

Дан целочисленный массив размера N. Назовем серией группу подряд идущих одинаковых элементов, а длиной серии – количество этих элементов (длина серии может быть равна 1). Вывести массив, содержащий длины всех серий исходного массива.

Задача №20

Заданы две матрицы А и В размером N×N. Сформировать из них прямоугольную матрицу Х размером N×2N, Включая в первые N столбцов матрицу А, в следующие – матрицу В.

Задача №21

Вычислить сумму и разность двух заданных одномерных массивов размером. Результат вывести на экран виде двух параллельных столбцов.

Задача №22

Вычислить сумму и разность двух заданных одномерных массивов размером 10. Результат вывести в виде двух параллельных столбцов

Задача №23

Задан массив А размером 6хМ и вектор В размером М. Элементы первого столбца массива А упорядочены по убыванию. Включить массив В в качестве новой строки в массив А с сохранением упорядоченности по элементам первого столбца.

Задача №24

Заданы квадратная матрица порядка N и число K. Разделить элементы k-строки на диагональный элемент, расположенный в этой строке.

Задача №25

Задан массив Х размером 10. Вычислить значение функции У=0,5 ln(x) при значениях аргумента, заданных в массиве Х, и поместить их в Массив У. Вывести на экран результат (массивы Х и У) в виде двух столбцов

Задача №26

Определить среднее значение элементов массива. Найти далее индекс элемента массива, наиболее близкого к среднему значению

Задача №27

В данном массиве поменять местами элементы, стоящие на нечетных местах, с элементами, стоящими на четных местах

Задача №28

Дана матрица размером a x b. Найти минимальное значение в каждой строке.

Задача №29

Дан массив размера N. Вывести его элементы в обратном порядке.

Задача №30

Заданную квадратную матрицу преобразовать, используя умножение строки на число и сложение строк, таким образом, чтобы все элементы первого столбца обратились в нуль, кроме элемента, расположенного на главной диагонали

Задача №31

Напишите программу, которая выводит список частот символов, встречающихся во входных данных

Задача №32

Дана квадратная матрица порядка М. Найти сумму элементов ее главной и побочной диагонали

Задача №33

Напишите программу, которая определяет число едениц в двоичной записи введенного числа N, 0  N  231

Задача №34

Элемент двухмерного массива называется локальным минимумом, если он строго меньше всех имеющихся у него соседей. Подсчитать количество локальных минимумов заданной квадратной матрицы порядка N Найти максимум среди всех локальных минимумов.

Задача №35

Заданы два одномерных массива различных размеров. Объединить их в один массив, включив второй массив между k -м и k+1 –ым элементами первого (k задано)

Задача №36
Матрица размещена в одномерном массиве по строкам. Поменять местами 1-ю и 2-ю строки матрицы. Результат вывести по строкам.
Задача №37
Задан целочисленный массив размера N. Определить максимальное количество его одинаковых элементов.
Задача №38
Дан одномерный массив А. Получить среднее арифметическое вех чётных элементов массива , стоящих на нечётных местах.
Задача№39
В массиве Х [1…20] каждый элемент равен 0, 1 или 2. Переставить элементы массива так ,чтобы сначала располагались все 0, затем 1, и, наконец, все 2. Задача№40
Даны два числа k1 и k2 и матрица размера a х b. Поменять местами столбцы матрицы с номерами k1 и k2
Задача№41
Дана вещественная матрица размерности n х m. По матрице получить логический вектор, присвоив его k-ому элементу значение True , если выполнено указанное условие и значение False иначе: - все элементы ; k столбца нулевые ; - элементы k строки матрицы упорядочены по убыванию; - k строка массива симметрична .

Задача №42

В одномерном массиве найти максимальный из отрицательных элементов и поменять его местами с последним элементом

Задача №43

Матрица размещена в одном массиве по строкам .Удалить k-й столбец матрицы из одномерного массива. Результат напечатать по строкам.

Задача №44

Определить ,имеются ли в целочисленном массиве C[1…20] два подряд идущих нулевых элемента.

Задача№45

Дана квадратная вещественная матрица размерности n. Найти количество нулевых элементов, стоящих : выше главной диагонали ; ниже главной диагонали ; выше и ниже побочной

Задача№46

Заполнить двухмерный массив T[…n,1…n] последовательными целыми числами от 1 до n, расположенными по спирали , начиная с левого верхнего угла и продвигаясь по часовой стрелке:

1 2 3 4 5 6

20 21 22 23 24 7

19 32 34 34 25 8



Задача №47

Найти среднее значение элементов заданного массива. преобразовать исходный массив, вычитая из каждого элемента среднее значение.

Задача №48

Сортировка чисел. Напишите программу, которая считывает натуральное число n, затем считывает n целых чисел и выводит эти числа в порядке возрастания.

Задача №49

Задана квадратная матрица. Переставить строку с минимальным элементом на главной диагонали со строкой с заданным номером.

Задача №50

Дан массив размера N. вывеси в начале его элементы с четными индексами, а затем с нечетными.

Задача №51

Вычислить сумму двух заданных матриц размером 55.

Задача №52

В заданной матрице заменить К-ую строку и L-й столбец нулями, кроме элементов, расположенного на их пересечении.

Задача№53

Задача матрица А размером NN. Сформировать два одномерных массива. В один переслать по строчкам верхний треугольник матрицы, включая элементы главной диагонали, в другой – нижний треугольник. Распечатать верхний и нижний треугольник по строкам.

Задача №54

Задан одномерный массив, содержащий различные численные значения. Требуется исключить из него одинаковые элементы.

Задача№55

Из заданной матрицы удалить 2-ю строку и 4-й столбец.

Задача№56

Найти сумму элементов одномерного массива размером 20. Разделить каждый элемент исходного массива на полученное значение. Результат получить в том же массиве

Задача№57

Напишите программу, которая переводит введенное число (в десятичной записи) в двоичную запись. Используйте рекурсивную функцию.

Задача№58

Матрица размещена в одномерном массиве по строкам. Удалить 1-ю строку матрицы из одномерного массива. Результат вывести по строкам.

Задача №59

Дана вещественная матрица размерности n  m. Сформировать вектор b, в котором элементы вычисляются как: - произведение элементов соответствующих строк; - среднее арифметическое соответствующих столбцов; - разность наибольших и наименьших элементов соответствующих строк; - значение первых отрицательных элементов в столбце.

Задача №60

Массив A [1...5,1..7] содержит вещественные числа. Требуется ввести целое число K и вычислить сумму элементов A [I , J], для которых I+J=K. Прежде, однако следует убедиться, что значение K позволяет найти решение, в противном случае нужно напечатать сообщение об ошибке.

Задача №61

Дан целочисленный массив размер N. Если он является перестановкой, то есть содержит все числа от 1 до N, то вывести 0, в противном случае ввести номер первого недопустимого элемента.

Задача №62

Заменить все отрицательные элементы целочисленного массива размер 10 на минимальное значение элементов массива.

Задача №63

Дан массив ненулевых целых чисел размера N. Проверить, чередуются ли в нем положительные и отрицательные числа. Если чередуются, то вывести 0, если нет, то вывести номер первого элемента, нарушающего закономерность.

Задача №64

В массиве А[1..N,1..N] определить номера строки и столбца какой нибудь седловой точки. Некоторый элемент массива называется седловой точкой, если он является одновременно наименьшим в своей строке и наибольшим в своем столбце.

Задача №65

Дана целочисленная матрица размера М х N. Найти количество ее строк и столбцов, все элементы которых различны.

Задача №66

Решить уравнение ах=b для пяти пар значений а и b, заданных в виде двух массивов. Результат поместить в массив Х.

Задача №67

Дано число k (0 < k < 11) и матрица размера 4 х 10. Найти сумму и произведение элементов k-го столбца данной матрицы.

Задача №68

Дан двухмерный массив А [1..m,1..n] . Написать программу построения одномерного массива B[1..m], элементы которого соответственно равны а) сумма элементов строк, б) произведениям элементов строк, в) наименьшим средних арифметических элементов строк.

Задача№69

Дан массив размера N, Определить количество его промежутков монотонности (то есть участков, на которых его элементы возрастают или убывают)

Задача№70

Просуммировать элементы строк матрицы размеров 8 × 6.Результат получить в одномерном массиве 8.

Задача№71

Задан целочисленный массив Х размерности n.Сжать массив, выбросив из него каждый второй элемент. Освободившиеся места в правой части массива заполните нулями. Дополнительный массив не использовать.

Задача№72

Задан массив Х размером 8.Сформировать из него матрицу А, содержащую по 3 элементов в строке. Недостающие элементы в последней строке(если такие будут) заполнить нулями. Напечатать матрицу по строкам.

Задача№73

Расположить элементы данного массива в обратном порядке (первый элемент меняется с последним, второй с предпоследним и т.д. до до середины; если массив содержит нечетное количество элементов,то средний остается без изменения).

Задача№74

Заданную квадратную матрицу преобразовать, используя умножение столбца на число и сложение столбцов, таким образом, чтобы все элементы первой строки обратились в нуль, кроме элемента, расположенного на главной диагонали.

Задача№75

Дан массив размера N.Определить количество участков, на которых его элементы монотонно возрастают.

Задача№76

Получить [aij]i=1,…10; j=1,…12 – целочисленную матрицу, для которой aij=i=2j.

Задача№77

Задана квадратная матрица. Исключить из неё строку и столбец, на пересечение которых расположен минимальный элемент главной диагонали.



Задача №78

Дан целочисленный массив размера N. Вывести вначале все его четные элементы, а затем – нечетные

Задача №79

Напишите программу, которая среди данных целых чисел ai , I = 1, …,

N находит три различных самых больших. Решите задачу однопроходным алгоритмом, без использования массивов и сортировки.

Задача №80

Дано натуральное число n. Получить действительную матрицу [a i j]

I, j=1 , …n, для которой a I j= .

Задача №81

Найти наибольшее из всевозможных попарных произведений элементов массива A[1…20].

Задача № 82

Для заданной квадратной матрицы сформировать одномерный массив из ее диагональных элементов. Найти след матрицы, суммируя элементы одномерного массива.

Задача№83

Дана целочисленная матрица размера a x b. Вывести номер ее первой строки, содержащего равное количество положительных и отрицательных элементов (нулевые элементы не учитываются). Если таких строк нет, то вывести 0.

Задача№84

Матрица размещена в одномерном массиве по строкам. Поменять местами 1-й и 2-й столбцы матрицы. Результат вывести по строкам.

Задача№85

Задан массив размером 20 . Сформировать два массива размером 10, включая в первый элементы исходного массива с четными индексами, а во второй – с нечетными.

Задача №86

Дан массив размера N и число k (0 < k < 5, k < N). Осуществить циклический сдвиг элементов массива влево на k позиций.

Задача №87

Вычислить скалярное произведение двух векторов (X, Y) размером 4.

Задача №88

Дан массив A[1…20] . Написать программу определения номера и значения первого элемента массива, который больше заданного числа С. Предполагается, что такой элемент существует.

Задача №89

В одномерном массиве размещены : в первых N элементах значения аргумента в порядке возрастания, в следующих – соответствующие им значения функции, и задана пара чисел – значения аргумента и функции. Поместить их в массив с сохранением упорядоченности по значениям аргумента. Напечатать полученный массив в виде двух параллельных столбцов (аргумент и функция).

Задача №90

Заданы матрица и вектор. Получить их произведение. Напечатать в строку.

Задача №91

Проверить, образуют ли элементы целочисленного массива размера N арифметическую прогрессию. Если да, то вывести разность прогрессии, если нет – вывести 0.

Задача №92

Заданы матрица (размером NN) и число 2. строку с максильным по модулю элементом во 2-ом столбце переставить со 2-ой строкой.

Задача №93

Дана квадратная матрица порядка M. Заменить нулями элементы матрицы, лежащие а) ниже главной диагонали, б) выше главной диагонали, в) ниже побочной диагонали.

Задача №94

Найти все локальные минимумы массива A[1…20]. Локальные минимумы – это элементы массива, которые меньше двух рядом стоящих с ним элементов. Например, в массиве 7 4 8 3 6 5 3 2 два локальных минимума – 4 и 3.

Задача №95

Дан целочисленный массив размера N. Преобразовать его, прибавив к четным числам первый элемент. первый и последний элемент массива не изменять.

Задача №96

Вычислить длину вектора X размером 4. Длина вектора – это корень квадратный из суммы квадратов его координат.

Задача №97

Поменять местами минимальный и максимальный элементы массива размера 10.

Задача №98

даны целые числа а1, а2, а3. Получить целочисленную матрицу [bij] i, j=1,2,3, для которой bij =ai-3aj.

Задача №99

Дано число k и матрица размером аb. Удалить столбец матрицы с номером k.

Задача №100

Дан одномерный массив N[1…20]. Все его элементы, не равные 0 переписать, сохраняя порядок в начало массива, а нулевые в конец массива (новый массив не заводить).